services:
    _defaults:
        autowire: true
        autoconfigure: true

    Kozmaoliver\EventMap\SymfonyEventMap\EventScanner:
        arguments:
            $scanDirectories: '%event_map.scan_directories%'
            $excludePatterns: '%event_map.exclude_patterns%'

    Kozmaoliver\EventMap\SymfonyEventMap\ListenerMapper:
        arguments:
            $eventDispatcher: '@event_dispatcher'
            $container: '@service_container'

    Kozmaoliver\EventMap\SymfonyEventMap\EventMapService: ~

    Kozmaoliver\SymfonyEventMap\Command\GenerateEventMapCommand:
        tags:
            - { name: console.command }
